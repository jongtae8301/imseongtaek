# Design System: InfoScope

## 1. Visual Theme & Atmosphere

차분한 실험 노트와 코드 편집기의 중간인 교육용 작업 공간. ‘정보가 너무 많아 복잡하다’는 사용자 요청을 우선하여 밀도 3, 비대칭 4, 움직임 1로 설계한다. 큰 시각화와 선택한 정보 탭만 기본으로 보여 준다. 예제·설정, 예상 질문은 필요할 때 펼친다.

taste-design의 정보 위계·색상·타이포그래피 규칙을 현재 React 앱에 적용한다. 외부 Stitch 호출은 사용하지 않는다. 교육용 관찰과 간소화 요청에 맞춰 사진 헤드라인·영구 애니메이션은 생략하고, 알고리즘 상태의 의미색은 유지한다.

## 2. Color Palette & Roles

- 종이 바탕 #F5F6F5: 앱 배경.
- 작업 면 #FFFFFF: 시각화와 선택한 패널.
- 본문 #242B28: 제목과 주요 값.
- 보조 잉크 #64716B: 단위·설정 설명.
- 구분선 #DEE4E0: 면의 경계와 행 구분.
- 짙은 녹색 #21634C: 선택·포커스·다음 버튼의 UI 강조색.

알고리즘의 현재 위치·변경·대기·LCA 색은 의미를 전달하는 데이터 색이며 텍스트·기호를 함께 쓴다.

## 3. Typography Rules

로컬 한국어 산세리프(Pretendard·맑은 고딕)와 Segoe UI, 코드는 Cascadia Code·Consolas를 쓴다. 외부 글꼴을 요청하지 않는다. 제목 20–26px, 본문·코드 14px 이상, 보조 정보·작은 화면의 조작 12–13px. 코드와 집계는 고정폭/고정 너비 숫자를 사용한다. Inter, 세리프, 과장된 영문 헤드라인과 장식 배지를 쓰지 않는다.

## 4. Component Stylings

‘예제·설정’으로 입력과 옵션을 펼친다. 직접 조작의 삽입·삭제는 기본 화면에 유지한다. 재생 제어의 강조 버튼은 ‘다음’ 하나이며 자동 실행은 보조 버튼이다. 주요 조작 높이 44px, 보조 도구 36px 이상, 2px 포커스 윤곽, 14px 패널 모서리와 얇은 경계. 중첩 카드·영역 번호·장식 아이콘을 줄인다. 오류와 입력 변경 안내는 설정 밖에서도 보인다. ‘예상해 보기’에서 학습 질문을 연다.

그래프 그림 편집 도구는 설정 안에 둔다. 미로는 사용자 요청에 따라 코드 없는 비교 전용 화면으로 구성한다. 두 미로를 항상 나란히 두고 공통 편집·재생 도구를 바로 노출한다. 어느 그림에서든 같은 미로를 수정하고, 각 그림 아래에는 발견한 칸 수와 경로 길이만 표시한다. 별도의 세 번째 편집 그림·정보 탭·고정 재생 바는 쓰지 않는다. 768px 미만에서는 조작 가능한 칸 크기를 위해 두 그림을 세로로 배치한다. 벽은 짙은 회색, 발견한 칸은 옅은 면과 +, 새 칸은 진한 윤곽과 ●, 출구 경로는 녹색 면과 이동 순서 숫자로 구분한다.

## 5. Layout Principles

시각화가 넓은 비대칭 2열. 오른쪽 정보 탭은 코드·현재 상태·자료구조·실행 기록·분석이다. ‘모두 펼치기’로 전체 영역을 확인할 수 있다. 모든 패널은 숨겨져도 같은 Step을 받는다. 900px 이하에서는 시각화를 포함한 여섯 탭 중 하나만 표시한다. 탭과 4개 학습 메뉴는 줄바꿈하고 화면 전체의 가로 넘침을 막는다. 패널은 내용 높이만큼 늘어나고 페이지 전체가 세로로 스크롤된다. 큰 코드·구조·표만 이름이 있는 내부 가로 스크롤을 허용한다. 기록은 최근 8개를 기본으로 표시하고 이전 기록을 펼칠 수 있다. 하단 고정 재생 바의 실제 높이만큼 페이지 끝에 여백을 확보한다. 최대 작업 폭은 1440px이다.

## 6. Motion & Interaction

버튼·탭에만 140ms 색상 변화와 눌림 1px을 적용한다. 실행 중 값·코드에 장식 애니메이션을 추가하지 않는다. 탭·설정은 인덱스·집계·재생 상태를 바꾸지 않는다. 표시 선호는 앱 메모리에서만 유지하고 새로고침하면 기본값으로 돌아간다. reduced-motion에서는 전환을 제거한다.

## 7. Anti-Patterns (Banned)

- 기본 화면에 여섯 패널과 모든 설정을 동시에 펼치지 않는다.
- 실습 제목·분류·안내를 배지와 문단으로 반복하지 않는다.
- 같은 크기 카드 세 개를 기본 작업 배치로 사용하지 않는다.
- 기능·예제·학습 안내를 삭제해서 화면을 비우지 않는다.
- 가짜 통계·외부 이미지·학생 추적을 추가하지 않는다.
- 코드와 상태 의미·집계·한도·검증 규칙을 시각 디자인 때문에 바꾸지 않는다.
